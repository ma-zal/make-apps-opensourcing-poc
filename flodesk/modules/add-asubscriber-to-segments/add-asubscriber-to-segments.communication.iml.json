{
	"url": "/subscribers/{{parameters.id_or_email}}/segments",
	"method": "POST",
	"body": {
		"segment_ids": "{{parameters.segment_ids}}"
	},
	"response": {
		"output": {
			"{{...}}": "{{omit(body, 'optin_timestamp', 'created_at')}}",
			"optin_timestamp": "{{parseDate(body.optin_timestamp)}}",
			"created_at": "{{parseDate(body.created_at)}}"
		}
	}
}